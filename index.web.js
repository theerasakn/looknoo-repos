import { AppRegistry } from 'react-native';
import App from './App';

AppRegistry.registerComponent('PawTrip', () => App);
AppRegistry.runApplication('PawTrip', {
  rootTag: document.getElementById('root'),
});
